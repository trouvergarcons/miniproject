{
  "name": "veracity-ai-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "npx kill-port 3000 || true",
    "prefrontend:dev": "npx kill-port 3000 || true",
    "dev": "concurrently --kill-others-on-fail \"npm:backend:run\" \"npm:frontend:dev\"",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend_django && python -m pip install -r requirements.txt",
    "install:backend:dev": "cd backend_django && python -m pip install -r requirements-dev.txt",
    "frontend:dev": "cd frontend && npx next dev --hostname 127.0.0.1 --port 3000 --webpack",
    "frontend:build": "cd frontend && npm run build",
    "backend:run": "cd backend_django && python -m pip install --upgrade pip || true && python -m pip install -r requirements.txt || true && python manage.py runserver 127.0.0.1:8000",
    "test:backend": "cd backend_django && python -m pytest",
    "start:all": "npm run backend:run"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "kill-port": "^2.0.1"
  }
}
